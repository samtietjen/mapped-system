{
  "name": "mapped-classes",
  "version": "1.1.0",
  "description": "Convert objects into consistent class name strings",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/samtietjen/mapped-system.git"
  },
  "scripts": {
    "build": "NODE_ENV=production babel src -d dist --config-file '../../babel.config.js'",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.1.0"
  }
}
